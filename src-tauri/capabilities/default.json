{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Kite desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "window-state:default"]
}
